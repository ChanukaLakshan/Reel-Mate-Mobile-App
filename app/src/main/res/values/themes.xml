<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NewReelMate" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/background_secondary</item>
        <item name="android:navigationBarColor">@color/background_secondary</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:enforceStatusBarContrast" tools:targetApi="q">false</item>
        <item name="android:enforceNavigationBarContrast" tools:targetApi="q">false</item>
        <!-- Apply dark yellow tint to ALL buttons globally -->
        <item name="android:buttonStyle">@style/DarkYellowButton</item>
        <item name="materialButtonStyle">@style/DarkYellowMaterialButton</item>
    </style>

    <style name="Theme.NewReelMate" parent="Base.Theme.NewReelMate" />

    <!-- Global Button style: yellow background tint, black text -->
    <style name="DarkYellowButton" parent="android:Widget.Button">
        <item name="android:backgroundTint">@color/primary_yellow</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <!-- Material Button style used by <Button> in Material3 theme -->
    <style name="DarkYellowMaterialButton" parent="Widget.Material3.Button">
        <item name="backgroundTint">@color/primary_yellow</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textAllCaps">false</item>
        <item name="cornerRadius">24dp</item>
    </style>

    <style name="CircleImageView" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>
</resources>